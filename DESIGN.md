# TopicAI DESIGN.md — 晨风 Morning Air（校准版）

**版本**：v2.0 · 2026-08-30
**状态**：正式设计规范。Phase 1 起所有前端实现以本文档为准；UI 走查与 code review 引用本文的 Pre-Flight 清单。
**制定依据**：`design-taste-frontend` 技能审计（判读/拨盘/AI-tell 清单）+ 产品负责人逐项拍板（色族=A 暖纸保留；骨架=晨风七界面；对话=三层模型）。
**实现载体**：MUI 5 主题令牌（`frontend/src/styles/theme.ts`），禁止平行手写 CSS 体系。

---

## 0. 设计判读（Design Read，永久有效，重大转向时才修订）

> TopicAI 是给消费级个体创作者的多步骤产品界面，带一个编辑性时刻（晨报=一封信）与一个营销面（登录/未来官网）。语言：安静纸面 + 单一强调色 + 结构化留白。产品面克制（低变化、低动效、中密度），编辑面允许排版呼吸，营销面按落地页纪律发挥。

**品牌理由（色彩例外条款的正式声明）**：暖纸底色属于 design-taste-frontend 技能点名的"AI 默认暖米色域"，本规范引用其例外条款予以保留——理由：TopicAI 是中文知识/经验创作者的书写与养成工具，"纸"是内容创作的母隐喻，暖纸底色与产品灵魂（书写、纸墨、信件式晨报）存在具体的品牌咬合，而非无意识默认。此决定为产品负责人 2026-08-30 拍板（选项 A）。

## 1. 适用面与三档拨盘

| 界面 | DESIGN_VARIANCE | MOTION_INTENSITY | VISUAL_DENSITY | 说明 |
|---|---|---|---|---|
| 产出架 / 拾取 / 收件箱 / 急稿 / 周复盘 / 成长 | 3 | 2 | 4–5 | 结构可预期是工作流信任的来源；动效仅用于状态反馈 |
| 晨报（编辑时刻，含对话流） | 4–5 | 3 | 2 | 它是一封信：排版呼吸、衬线、允许极少量 emoji |
| 登录 / 未来官网 | 7 | 6 | 3 | design-taste-frontend 完整管辖面，按其全量清单执行 |

## 2. 色彩令牌

一处强调色（草木绿），一处次强调（杏，仅探索位），语义色各一。全站锁定，任何界面不得引入第二强调色。

| 令牌 | 值 | 用途 |
|---|---|---|
| `background.default` | `#F8F7F3` | 应用底（暖纸）※例外声明见 §0 |
| `background.paper` | `#FFFFFF` | 卡面 |
| `background.soft` | `#F1EFE9` | 次级底（判断草案卡、空态） |
| `text.primary` | `#1F1E1B` | 主文字（近黑，禁纯黑） |
| `text.secondary` | `#3F3E3A` | 正文 |
| `text.muted` | `#83817A` | 次要 |
| `text.faint` | `#B5B3AA` | 极淡/占位 |
| `divider` | `#ECEAE3` | 发丝线 |
| `primary.main` | `#4A7C6B` | 草木绿：主按钮、激活态、链接、能力条 |
| `primary.dark` | `#3B6557` | hover/pressed |
| `primary.contrastText` | `#FFFFFF` | 对比度 4.8:1，达 AA |
| `primary.wash` | `#EBF1ED` | 激活底、消息气泡 |
| `secondary.main` | `#C98A4B` | 杏：**仅**探索位标记 |
| `secondary.wash` | `#F7EFE5` | 探索位底 |
| `error` | `#B4574E` | 错误 |
| `warning` | `#B98A2F` | 警示（少用） |
| `info` | `#4A6FA5` | 信息（少用） |

**纪律**：禁纯黑 `#000`、禁纯白大面积底（卡面白是容器不是页面底）；暖灰体系内禁混入冷灰；无渐变底色；无霓虹外发光。深色模式：v1 锁浅色，暗色令牌在 Phase 5 补齐（`prefers-color-scheme`），届时维持品牌可识别与 AA 对比。

## 3. 排版

### 3.1 字族与衬线隔离（本规范最重要的一条排版纪律）

- **产品面全部无衬线**。衬线只允许出现在**编辑时刻**：晨报问候语、管家的"信件体"消息、登录页品牌语。理由（技能要求的 articulation）：这些时刻是"内容"而非"界面 chrome"——晨报是一封信，信用衬线是文体，不是装饰。
- 中文栈：`-apple-system, "PingFang SC", "Microsoft YaHei", "Noto Sans SC", sans-serif`（系统栈，零下载）。
- 西文/数字栈：v1 用系统栈；Phase 5 可选自托管 `Geist`（@font-face + font-display:swap），禁止 `<link>` 外链 Google Fonts。
- 衬线栈（仅编辑时刻）：`Georgia, "Songti SC", "Noto Serif SC", "SimSun", serif`。
- 禁止 Fraunces / Instrument Serif。西文强调用同族 italic/bold，禁止混族强调。

### 3.2 字阶

| 级别 | 规格 | 用途 |
|---|---|---|
| display | 32px / 1.4 / 衬线 | 晨报问候 |
| h1 | 30px / 600 / -0.5px | 页标题（无衬线） |
| h2 | 20px / 500 | 拾取面板标题、卡片主题 |
| h3 | 16px / 600 | 卡片标题 |
| body | 15px / 1.7 | 正文（界面 chrome 用 1.5） |
| caption | 13px | 元信息 |
| micro | 11px / 600 / +2px / 大写 | eyebrow——**配额制：每 3 个区块最多 1 个** |

斜体展示字含降部字母（y g j p q）时行高 ≥1.1 并预留底部空间。中文破折号"——"禁作分隔装饰，仅保留正常汉语标点用法且逐句复查。

## 4. 形状、影与密度

- **圆角锁（documented mixed rule）**：交互元素=全圆 pill；卡片/面板=18px；输入框/内嵌块=12px；标签=全圆。此规则全站无例外。
- **阴影**：`0 1px 2px rgba(31,30,27,.04), 0 8px 28px rgba(31,30,27,.05)`（着色于底色，禁纯黑影）。卡片优先靠发丝线分组，影子只在真正悬浮（粘性拾取面板、展开的长杆）时使用。
- 密度：MUI density=comfortable；间距基单元 8px；区块间距 ≥40px；内容最大宽 1120px。

## 5. 图标与符号

- 图标库：`@phosphor-icons/react`，全局 strokeWidth 1.5，一族到底，禁手绘 SVG 图标。
- **emoji 政策**：产品面禁用 emoji 作图标；编辑时刻（晨报、里程碑）允许极少量有意图使用（每屏 ≤2）。
- **装饰性圆点禁**。唯一可留的点是真实语义状态灯（如"AI 正常"），每区至多一个。
- 中点 `·` 分隔符每行最多 1 个，超出的改用空格分组或换行。
- 能力/进度展示：数字 + 无底轨微条（禁带灰底轨的填充进度条）。

## 6. 组件规范（v1 集合）

| 组件 | 规则 |
|---|---|
| 主按钮 | pill，`primary.main` 底白字，:active 下沉 `translateY(1px)`；文案 ≤6 字不折行 |
| 次按钮/文字按钮 | 发丝线 ghost / 无底 text（hover 变绿） |
| 产出卡 | 白底发丝线，hover 边框变绿；标签行无圆点；探索位用杏色"尝试"标 |
| 拾取面板 | 粘性、白底、18px 圆角；事实清单行 = 陈述 + 灰色溯源 + 右侧"改" |
| 判断草案卡 | `soft` 底 12px 圆角，无新增强调色 |
| **对话三层** | L1 主页对话流（AI 消息=衬线正文+头像+内嵌卡片，用户消息=绿染气泡右对齐，快捷意图≤3）；L2 对象内问答（就地展开推开内容，线程附着对象，提议一律"待采纳"）；L3 长杆（每屏顶部结构组件，⌘K，内联下拉非浮层，上下文页脚标注） |
| 对话红线 | 对话内 AI 仅可回答/提议/执行可逆操作；拾取、公开范围、发布、长期经验四个决策必须用户亲手点击确认 |
| 状态 | 空=引导性构成；加载=匹配最终布局的骨架屏（禁转圈）；错误=就地+重试 |
| 动效 | 150–200ms ease；仅 transform/opacity；`prefers-reduced-motion` 全量降级；每条动效须能一句话说出动机 |
| z-index | 仅四层：内容 0 / 粘性 10 / 长杆展开 20 / 登录遮罩 50 |

## 7. Pre-Flight 清单（UI 走查/合并前逐项过）

1. 设计判读与本规范一致；拨盘未越面。
2. 衬线只在编辑时刻出现。
3. 全站仅草木绿强调 + 杏探索标；无新色；暖灰未混冷灰。
4. 圆角锁无违例；阴影着色无纯黑。
5. 无 emoji 图标（编辑时刻 ≤2）；无装饰圆点；中点每行 ≤1；无 em/双连破折号作分隔。
6. 按钮对比 AA（白字绿底 4.8:1 ✓）；CTA 不折行；同意图 CTA 全站一个文案。
7. 输入框 placeholder/焦点环/错误文案对比 AA；placeholder 不当 label。
8. 动效皆有动机且 reduced-motion 降级；无 scroll 监听器直连。
9. 空/加载/错误三态齐备；骨架屏形状匹配。
10. 中文文案自审：无语病、无指代不明、无"AI 腔"抒情；数字非伪造精度。
11. 深色模式未启用前，不得出现半深色块。

## 8. 与既有文档的关系

- 领域词汇：`CONTEXT.md`（仅词汇，不含实现）。
- 产品方案：`docs/ai-native-async-creation-plan-2026-08-29.md`。
- 本文档取代所有原型 HTML 中的内联样式决定；原型仅作交互演示留存。
