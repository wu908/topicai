# TopicAI DESIGN.md — 琉璃 Lumen v3

**版本**：v3.0 · 2026-08-30（晨风暖纸 → 琉璃白玻璃全面翻新；对话系统定稿为「悬浮球 + π 形灯框 + 独立输入泡」）
**状态**：正式设计规范。所有前端实现以本文为准；UI 走查与 code review 引用 §9 Pre-Flight 清单。
**实现载体**：MUI 5 主题令牌（`frontend/src/styles/theme.ts`）；动效编排用 GSAP（`npm i gsap`，React 侧按 `useGSAP` 模式），环境循环允许 CSS。
**演化记录**：v1 晨风暖纸衬线 → v2 白玻璃去衬线 → v3 去绿色、科幻白玻璃、对话按产品负责人手绘稿定稿。

---

## 0. 设计判读（Design Read）

> TopicAI 是给消费级个体创作者的多步骤产品界面，带一个编辑性时刻（晨报信）与一个营销面（登录）。语言：**冷调虚幻空间上的半透明白玻璃**，光本身（冰蓝白）是唯一"彩色"，界面主体无彩。产品面克制，编辑面呼吸，营销面按落地页纪律发挥。

## 1. 拨盘

| 界面 | VARIANCE | MOTION | DENSITY |
|---|---|---|---|
| 产品面（产出架/拾取/收件箱/急稿/复盘/成长） | 3 | 2–3 | 4–5 |
| 晨报（编辑时刻） | 4–5 | 3 | 2 |
| 登录 / 官网 | 7 | 6 | 3 |

## 2. 色彩

无彩色玻璃体系 + 单一"光色"（仅用于光效，不作 UI 强调色）。**绿色已全面移除。**

| 令牌 | 值 | 用途 |
|---|---|---|
| `bg.field` | `#F1F4F9 → #E7EBF3` 渐变 + 漂移光晕层 | 虚幻背景 |
| `bg.glass` | `rgba(255,255,255,.55)`（卡）/ `.34`（浮窗）/ `.5`（输入泡） | 所有表面，blur 26–30px saturate 150–160% |
| `ink` | `#191E26` | 主文字（近黑，禁纯黑） |
| `body` / `muted` / `faint` | `#454E5C` / `#7E8898` / `#AEB6C4` | 次级文字 |
| `line.glass` | `rgba(255,255,255,.8)` + outline `rgba(23,28,38,.055)` | 浅色双边界（外白内灰） |
| `light` | `#8FBEE8` + 白 | **仅**灯带/弧环/辉光/焦点环 |
| `explore` | `#C98A4B` 系杏色（保留） | 仅探索位标记 |
| 语义 | error `#B4574E` / warning `#B98A2F` | 少用 |

纪律：单强调色原则以"光色只在光效"的形式执行；暖冷灰不混（本体系为冷调）；无紫渐变、无霓虹按钮光、无纯黑白大面积。

## 3. 排版

- **全站无衬线**（衬线自 v3 移除；晨报信以字重与大字号表达文体，不再用衬线）。
- 中文栈：`-apple-system, "PingFang SC", "Microsoft YaHei", "Noto Sans SC", sans-serif`（零下载）。
- 拉丁/数字：**Space Grotesk**（自托管，`font-display:swap`；演示页允许 CDN）。全站数字 `tabular-nums`。
- 字阶：display 40/800/-1.4px（晨报）；h1 32/800/-0.8px；h2 20/700；h3 16/700；body 15/1.7；caption 13；kicker 11 Space Grotesk 600 大写 +0.22em（**每 3 区块最多 1 个**）。
- 层级靠字重 + 颜色，不靠大小硬撑；斜体展示字含降部时行高 ≥1.1；禁"——"作分隔装饰。

## 4. 形状、材质与深度

- **圆角锁**：交互/胶囊=9999；卡片/面板=22px（外）/15–16px（内嵌）；输入泡=全圆。
- **Double-Bezel**：一切悬浮容器为"外亮边壳（白 .22–.3 + 白边界 + outline 灰发丝）+ 内核（blur 玻璃 + inset 顶部高光）"。
- **阴影**：冷调着色 `0 22px 60px rgba(70,95,130,.14)`；禁纯黑影。
- 背景三层：渐变场（固定）+ 2–3 个漂移光晕（transform 动画，reduced-motion 关）+ 极淡点阵。
- 卡片仅在表达层级时使用；否则用留白与发丝线分组。

## 5. 图标与符号

- 图标库 `@phosphor-icons/react`，strokeWidth 1.5，禁手绘 SVG 图标。
- emoji 默认禁；晨报/里程碑每屏 ≤2 处可保留。
- 装饰性圆点禁；真实语义状态灯（AI 正常）每区 ≤1。中点 `·` 每行 ≤1。

## 6. 对话系统（定稿：悬浮球 + π 形灯框 + 独立输入泡）

**结构三体分离**（产品负责人手绘稿定稿）：

1. **悬浮球**：80px 白玻璃球，六层构造——呼吸光晕 / 高光球体 / 内部能量涡旋（锥形渐变慢旋）/ 两条透视 3D 轨道环（74°/66° 反向异速，外环带发光卫星点）/ 中心 ✦ / 主动提问脉冲波。**可随意拖动**。
2. **π 形灯框**（消息区）：仅左、顶、右三条边，**底部开放不闭合**；玻璃底沿底部渐变遮罩消散；左右灯带 + 顶灯带为边界光，流光循环；头栏含上下文芯片与"渐隐/重播开场/关闭"。
3. **独立输入泡**：与消息框分离的白玻璃胶囊（输入 + 墨色圆形发送钮），悬于灯框下方 14px。

**开合与锚定**：
- 点击球 → 输入泡从贴球一侧伸出 → 双灯带自输入泡两端上升 → 顶部交会闭环 → 框内消息成像（四幕全息，GSAP 时间线：labels + position 参数，回退 CSS 四幕）。
- 四幕**仅首次启动/重启播放一次**（持久化标记）；之后开合为快速淡入。
- 堆叠**永远贴附悬浮球**：优先整体贴球上方（输入泡底距球顶 18px），放不下贴下方，最后钳制屏内；框体 max-height 按剩余空间自适应（下限 300）。
- **拖球时堆叠实时跟随**（显影状态下逐帧重算贴附）；窗口 resize 重贴附。
- **渐隐**：闲置 12s，chrome（灯框边界/头栏/页脚）渐隐至 5%，只剩消息气泡与输入泡；指针/焦点唤醒。关闭时清除 GSAP 内联残留（防复开隐形）。

**主动权**：AI 对用户想法的缺口、逻辑漏洞、缺漏事实**及时主动发问**（浮窗内推送 + 球体脉冲）；只问不改。拾取、公开范围、发布、长期经验四个决策必须用户亲手确认。

**情景注入**：任何"对话/询问"按钮打开同一浮窗并注入该对象上下文（芯片 + 带出处开场白）。

## 7. 红线（视觉执行）

1. 无预测/评分类视觉元素；2. 无自动发布暗示（定时=提醒）；3. 无虚构（事实清单必须可溯源展示）；4. 四决策 HumanGate 不可绕过；5. `/api/v2` only；6. TDD 覆盖 ≥80%；7. additive migration；8. AI 动效写 AITrace；9. 无死路（失败态可见可重试）；10. 素材授权最小化默认。

## 8. 动效总表

| 场景 | 编排引擎 | 规格 |
|---|---|---|
| 首启四幕 | GSAP timeline | ①输入泡 back.out(1.4) 0.5s ②灯带 power3.inOut 0.62s stagger .07 ③顶帽 power2.out 0.45s ④成像/玻璃 power1.out 0.55s |
| 球体入场 | GSAP | scale0→1 + rotation -120→0, back.out(1.6) 0.9s；光层 delay .35–.5 |
| 气泡浮现 | GSAP | y12/autoAlpha/scale .97→1, power3.out 0.45s |
| 开合（非首启） | GSAP | fromTo 0.32s power3.out / 收 0.22s power2.in |
| 环境循环 | CSS | 弧环 spin 5–9s、灯带 flow 3s、呼吸 5s、背景漂移 24–38s |
| 渐隐 | CSS | chrome opacity→.05, 1.2s ease |

全部仅 transform/opacity；`prefers-reduced-motion` 全量降级。

## 9. Pre-Flight 清单（走查/合并前逐项）

1. 判读与拨盘未越面；2. 无衬线（全站）；3. 色彩锁（白玻璃 + 冰蓝光效，无绿无紫）；4. 圆角锁与 Double-Bezel 无违例；5. 阴影冷调无纯黑；6. 无 emoji 图标（编辑时刻 ≤2）、无装饰圆点、中点每行 ≤1、无破折号分隔；7. 按钮/表单对比 AA；CTA 不折行；同意图 CTA 唯一；8. 动效有动机、reduced-motion 降级、无 scroll 监听直连；9. 空/加载（骨架）/错误三态齐备；10. 文案自审无语病与 AI 腔；11. 数字 tabular；12. 对话三体（球/框/泡）锚定与渐隐行为符合 §6；13. 关闭后无内联残留（可复开）。

## 10. 关系

- 领域词汇：`CONTEXT.md`；产品方案：`docs/ai-native-async-creation-plan-2026-08-29.md`；规格：`specs/013-async-creation-loop/`。
- 本文档取代所有原型内联样式；原型（`docs/prototypes/hifi-lumen.html`）仅作交互演示。
