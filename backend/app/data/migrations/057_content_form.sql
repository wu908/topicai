-- 057_content_form.sql（意图分类拆分的 Step 1：新增开放字段）
--
-- 「内容形态」：这条内容是什么样子——作品展示 / 教程 / 清单 / 观点 / 复盘 / 对比 / 故事…
-- 由模型读内容命名，**开放、不设枚举**；只用于展示与提示词上下文。
--
-- 与 content_intent（有限三值）的分工：
--   content_intent → 机器行为（路由、判断必填项、观察信号、以及"哪些已沉淀经验适用"）
--   content_form   → 给人看的名字（AI 可以给任何形态命名，用户不再从三个里选）
--
-- Step 1 刻意不接前端：这一列先只被写入、被认领交接，界面上不读它，
-- 所以这一步上线后线上零视觉变化（可单独回退）。
--
-- 加列由 runner / 内存路径的 ensure_columns 完成（PRAGMA 守卫，可重复执行）。
-- 注意：content_projects 会被 043 的重建迁移原样拷贝所有列，
-- 因此这一列必须同时加进 runner 的 _INTENT_MODEL_CONTENT_PROJECTS_SQL。
