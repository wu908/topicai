-- 056_digest_reader_draft.sql（消化器起草 solve 类的两个必填项）
--
-- 发布意图锁定（solve 类）要求两项：读者遇到什么问题 / 你准备给出的答案。
-- 系统此前不产出它们，用户必须自己写——而消化器已经读过素材、写过整篇草稿，
-- 这两句本来就在正文里（"我的读者不是来找结论的，而是想看你当时怎么想"）。
--
--   audience_problem  读者在真实场景里遇到的具体困境（消化器起草，可改）
--   reader_promise    你能基于亲身经历讲清楚的方法或步骤（消化器起草，可改）
--
-- 与 audience_change 同一性质：都是"草案"，存在项目上供锁定那一步预填，
-- 用户可以改；真正的记录仍是锁定时写入的 publish_hypothesis。
-- AI 不可用时保持 NULL——不编造，让用户自己写。
--
-- 加列由 runner / 内存路径的 ensure_columns 完成（PRAGMA 守卫，可重复执行）。
