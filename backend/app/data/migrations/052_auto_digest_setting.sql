-- 052_auto_digest_setting.sql (Spec-013 Phase 1 follow-up)
--
-- 夜间自动消化开关（默认关）。用户主动开启后，调度器每晚把收件箱里
-- 可发布的素材整理成待发布产出；私密素材永不参与。
--
-- 加列由 runner 的版本分支用 _ensure_columns 完成（PRAGMA 守卫，可重复执行），
-- 这里只记录语义——与 046 的做法一致。
