# 内容页双栏 1280/390 视觉走查（2026-09-22）

> 对象：生产 `http://1.13.251.252:8081/content`，HEAD `4591909`（双栏选中态/视图切换）。
> 造数：临时账号 + `/projects/start` 两条真实句子。截图：`browser-screenshots/content-visual/`。

## 结论

| 检查项 | 1280 | 390 | 处置 |
|---|---|---|---|
| 横向溢出 | 无 | 无 | — |
| 双栏 list+preview | 同排 | 上下堆叠 | 通过 |
| 视图 chips（全部/需要我/观察中/已完成） | 4 枚 | 4 枚 | 通过 |
| 搜索框「搜标题或形态」 | 有 | 有 | 通过 |
| 阶段条（意图…观察）+ 进度 1/5 | 有 | 有 | 通过 |
| 选中态 | 有 | 有 | 通过 |
| 右栏 CTA 与列表 `nextActionLabel` 同源 | 「先写下真实经历」 | 同 | 通过 |
| 选中 chip 文字对比度 | **「全部」填充实心下文字发糊** | 同 | **已修** |
| 390 底栏/FAB 压住「打开完整工作台」 | — | **遮挡** | 记账（见下） |

## 1. 选中视图 chip 文字对比度（已修）

截图上默认选中的「全部」chip 是浅底，文字几乎看不见（同排「需要我/观察中/已完成」为描边深字，正常）。
根因：主题 primary 填充色过浅，`filled` 态文字对比不足。
修法：选中态强制 `#191E26` 底 + 白字（`ContentPage.tsx` view chips）。

## 2. 390 底栏与悬浮球压住右栏 CTA（量化）

390 堆叠布局下，右栏「打开完整工作台」位于列表之后；滚到该区域时：

- 底栏 top≈773，CTA 文案被压在底栏下（需再滚一屏才能完整点到）
- 右下固定悬浮球压在 CTA 右侧留白，未压住字面

窄屏已有 `.app-main-content { padding-bottom: 180px }`，可滚出底栏；属固定底栏的中间态，
与素材页那次误判同类。**不改 padding**，记账即可。

## 3. 通过的细节

- 双栏断点：≥840 左列表 320–360 / 右预览；<840 上下堆叠
- 行内：意图标签 + 时间 + 五段 StageMeter（图标+文字+颜色）+「需要我」旗标
- 右栏：标题 / 阶段 / 目标 / 主按钮（与列表同源）/「打开完整工作台」
- 说明文字与按钮矩形重叠 2 对，均为 stage 条与行内 chip 的几何相交，非文字压字

## 验收基准（已满足）

- 1280/390 无横向溢出
- chips 可点且选中态文字对比 AA
- 点行 → 右栏预览；CTA 进工作台
- 列表与右栏「下一步」文案一致（G4）
