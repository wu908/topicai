/**
 * Writing page (V3 /writing route).
 * Aliased from /ideas for backward compatibility.
 * Renders the same functionality as IdeaBoosterPage.
 */
import React from 'react';
import IdeaBoosterPage from '@/pages/IdeaBooster/IdeaBoosterPage';

const WritingPage: React.FC = () => {
  return <IdeaBoosterPage />;
};

export default WritingPage;
